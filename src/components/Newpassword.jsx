import React from 'react'

function Newpassword() {
  return (
    <div>
      <input type="text" placeholder="Enter New Password" />
      <input type="text" placeholder="Conform New Password" />
          <button>Save</button>
    </div>
  );
}

export default Newpassword